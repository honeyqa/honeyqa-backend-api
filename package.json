{
  "name": "nodejs-restful-api-server",
  "version": "0.0.1",
  "description": "nodejs-restful-api-server",
  "author": "HoneyQA Team",
  "main": "index.js",
  "keywords": [
    "nodejs",
    "client",
    "javascript"
  ],
  "scripts": {
    "pretest": "jslint --node --sloppy index.js lib/*.js lib/jute/*.js",
    "test": "mocha --recursive --reporter spec test/*",
    "coverage": "istanbul cover _mocha --recursive test/*"
  },
  "engines": {
    "node": ">=0.6.0"
  },
  "dependencies": {
    "amqplib": "^0.4.0",
    "async": "~0.2.7",
    "body-parser": "^1.14.1",
    "express": "^4.13.3",
    "underscore": "~1.4.4"
  }
}
